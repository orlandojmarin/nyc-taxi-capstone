name: 'capstone'
version: '1.0.0'
config-version: 2

profile: 'capstone'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  capstone:
    staging:
      +materialized: table
      +schema: AMO_SILVER
    marts:
      +materialized: table
      +schema: AMO_GOLD
